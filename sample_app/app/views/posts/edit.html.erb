<!-- フォームに編集したいデータ入れるにはform_for利用。インスタ変数にあるモデル渡すだけで値を入れてくれる -->
<h1>編集画面</h1>
<%= form_for(@post) do |f| %>
	<h4>タイトル</h4>
	<% f.text_field :title %>
	<h4>本文</h4>
	<%= f.text_area :body %>
	<%= f.submit '保存' %>
<% end %>