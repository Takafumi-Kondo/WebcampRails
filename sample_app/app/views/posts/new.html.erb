<h1>新規投稿</h1>
<!-- form_forの部分(htmlのformタグを作成する機能) -->
<%= form_for(@post) do |f| %>

	<h4>タイトル</h4>
	<%= f.text_field :title %>

	<h4>本文</h4>
	<%= f.text_area :body %>

	<!-- 画像gem追加後追加 -->
	<h4>画像</h4>
	<%= f.attachment_field :image %>
	<!-- ↑↑ -->

	<%= f.submit '投稿' %>

<% end %>